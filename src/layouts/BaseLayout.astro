---
import '../styles/global.css';
import ExampleDataBanner from '../components/ExampleDataBanner.astro';
import { getMeta } from '../lib/dump';

interface Props {
  title?: string;
  description?: string;
  /**
   * Override example banner. Default: dump `meta.is_example`.
   * Pass explicitly only for tests or special pages.
   */
  showExampleBanner?: boolean;
}

const meta = getMeta();
const {
  title = 'Evidence Desk',
  description = 'Search and browse the OSMF Shared Evidence Graph.',
  showExampleBanner = meta.is_example,
} = Astro.props;

const pathname = Astro.url.pathname;
const isHome = pathname === '/' || pathname === '/index.html';
const isSearch = pathname.startsWith('/search');
const isAbout = pathname.startsWith('/about');
const isDownload = pathname.startsWith('/download');
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body>
    <div class="layout-root">
      <ExampleDataBanner show={showExampleBanner} />
      <header class="site-header">
        <div class="site-header__inner">
          <a class="site-brand" href="/">Evidence Desk</a>
          <nav aria-label="Primary">
            <ul class="site-nav">
              <li>
                <a href="/" aria-current={isHome ? 'page' : undefined}>Home</a>
              </li>
              <li>
                <a href="/search" aria-current={isSearch ? 'page' : undefined}
                  >Search</a
                >
              </li>
              <li>
                <a href="/about" aria-current={isAbout ? 'page' : undefined}
                  >About</a
                >
              </li>
              <li>
                <a
                  href="/download"
                  aria-current={isDownload ? 'page' : undefined}>Download</a
                >
              </li>
            </ul>
          </nav>
          <form
            class="site-search"
            action="/search"
            method="get"
            role="search"
            aria-label="Search entities"
          >
            <label class="visually-hidden" for="site-search-q">Search entities</label>
            <input
              id="site-search-q"
              class="site-search__input"
              type="search"
              name="q"
              placeholder="Search entities…"
              autocomplete="off"
            />
            <button class="site-search__submit" type="submit">Search</button>
          </form>
        </div>
      </header>
      <main class="site-main" id="main">
        <slot />
      </main>
      <footer class="site-footer">
        <div class="site-footer__inner">
          <p class="not-medical-advice">Not medical advice.</p>
          <p>
            Evidence Desk surfaces curated research claims and sources. It does
            not provide clinical recommendations or dosing guidance. Always
            consult a qualified clinician for personal health decisions.
          </p>
        </div>
      </footer>
    </div>
  </body>
</html>
